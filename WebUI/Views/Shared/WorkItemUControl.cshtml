@using CameraMap.App_GlobalResources
@model CameraMap.Models.UserControl.UserCtrlMode
<script type="text/javascript">
    $(function () {
        //$("select").wijdropdown();
        $("select").wijcombobox({
            isEditable: false
        });
    });
</script>
@Html.DropDownListFor(m => m.WorkItemMode.WorkItemGuid, Model.WorkItemMode.WorkItemList, new { @style = "width:150px;height:30px;" })
